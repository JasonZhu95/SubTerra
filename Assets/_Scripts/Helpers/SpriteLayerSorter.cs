using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

//#if UNITY_EDITOR
//[CustomEditor(typeof(Transform))]
//public class SpriteLayerSorterEditor : Editor
//{
    //public override void OnInspectorGUI()
    //{
    //    base.OnInspectorGUI();

    //    if (GUILayout.Button("Sort Child Objects"))
    //    {
    //        Transform selectedTransform = (Transform)target;
    //        for (int i = 0; i < selectedTransform.childCount; i++)
    //        {
    //            selectedTransform.GetChild(i).transform.position += new Vector3(0, 0, 0.001f * i);
    //        }
    //    }

    //}
//}
//#endif
